> pnpm add -D @types/node
> pnpm add kafkajs
> docker compose up -d

# Tao KAFKA_KRAFT_CLUSTER_ID

      KAFKA_KRAFT_CLUSTER_ID: xK9mQr2sLpVbN8hTcYwEzA

> docker run --rm bitnami/kafka:latest kafka-storage.sh random-uuid
> **ZooKeeper đã bị Kafka loại bỏ hoàn toàn từ phiên bản 3.8+, và KRaft (Kafka Raft Metadata mode) là tương lai duy nhất của Kafka.**
> check 8080

# redis ui

> http://localhost:5540
