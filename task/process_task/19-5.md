# Task Log – 2026-05-19

## ✅ COMPLETED: Fix Recommendation Pipeline Bug

### Root Causes Found (8 layers)

| #   | Root Cause                                         | File                                        | Fix                                                        |
| --- | -------------------------------------------------- | ------------------------------------------- | ---------------------------------------------------------- |
| 1   | `trackInteraction()` arg order swapped             | `StayDetailPage.tsx`                        | `trackInteraction(stayData.id, "VIEW")` (was reversed)     |
| 2   | VIEW only tracked on unmount after 5s              | `StayDetailPage.tsx`                        | Track VIEW immediately on page load                        |
| 3   | `POST /api/interactions` 400 error                 | `route.ts`                                  | Added hotelId coercion, type validation, raw body logging  |
| 4   | Time decay = 0.0 for >30min interactions           | `recommend.py`, `get-ai-recommendations.ts` | Never return 0.0 (min 0.1)                                 |
| 5   | Session decay too short (30 min)                   | Both files                                  | Extended to 2 hours                                        |
| 6   | Intent threshold too high (needed 2+ high-intent)  | `get-ai-recommendations.ts`                 | Lowered to 1+, added PRIORITY 2.5 for any interaction type |
| 7   | Search-service IDs bypassed intent filtering       | `get-ai-recommendations.ts`                 | Skip candidateIds when any session intent detected         |
| 8   | Prisma fallback hard-filtered by stale destination | `get-ai-recommendations.ts`                 | Simplified: fetch diverse pool first, then filter          |

### Files Modified

| File                                                             | Changes                                                          |
| ---------------------------------------------------------------- | ---------------------------------------------------------------- |
| `apps/client/src/pages/StayDetailPage.tsx`                       | Fix arg order, track VIEW immediately                            |
| `apps/client/src/app/api/interactions/route.ts`                  | HotelId coercion, type validation, debug logs                    |
| `apps/client/src/actions/get-ai-recommendations.ts`              | Simplified: direct Prisma query, intent boost, 4-hotel selection |
| `apps/client/src/components/home/ai-recommendations-section.tsx` | Hydration fix, 12-col grid layout (4 cards), debug logs          |
| `apps/search-service/src/recommend.py`                           | Hard session routing, time decay, session decay                  |

### Grid Layout (Final)

```
┌──────────────────┬─────────────────┐
│                  │    Hotel 1      │
│   Hotel 0        │   (6 cols)      │
│  (6×2 featured)  ├────────┬────────┤
│                  │Hotel 2 │Hotel 3 │
│                  │(3 cols)│(3 cols)│
└──────────────────┴────────┴────────┘
```

### Debug Logs Added

- Terminal: `[recommend]`, `[intent]`, `[interactions]`
- Browser: `[ai-recommend] 📞`, `📥`

---

## ✅ COMPLETED: AI Personalized Section UI

- 12-column CSS grid with varied card spans
- 4 hotels displayed (was 3)
- Featured card (6 cols × 2 rows) + 3 smaller cards
- Destination-aware selection (session intent → matching hotels first)
