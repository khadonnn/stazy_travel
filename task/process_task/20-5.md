Nói thẳng: hiện tại hệ thống của bạn đang bị 3 vấn đề kiến trúc, không còn là “tuning SVD 60/40” nữa.

Vì sao bạn vẫn thấy “Hà Nội / cache HIT / UI không đổi”?
Log của bạn đã tố cáo rõ:

[cache] HIT userId=... dest=Vũng Tàu
CACHED RESULT

👉 Nghĩa là:

UI KHÔNG hề chạy lại pipeline recommendation
→ nó chỉ đang lấy kết quả cache cũ

Và quan trọng hơn:

cache key của bạn đang “dính intent cũ + fingerprint cũ” nên không invalidate khi bạn click khách sạn mới

Vì sao chọn Vũng Tàu nhưng UI vẫn ra Hà Nội / Đà Nẵng / Huế?
Có 2 lớp lỗi:

(A) Intent đúng, nhưng KHÔNG được dùng để rebuild result

Log:

[intent] RECENT SHIFT: "Vũng Tàu"

nhưng sau đó:

[cache] HIT ... dest=Vũng Tàu → CACHED RESULT

👉 tức là:

intent mới = Vũng Tàu
nhưng system vẫn trả result của request trước đó

(B) Cache key sai logic (root cause thật)

Bạn đang cache theo kiểu:

userId
destination
fingerprint

Nhưng fingerprint của bạn:

last 5 interactions (VIEW-heavy)

👉 VIEW là signal yếu + dễ “kẹt lịch sử”

=> nên fingerprint không đổi đủ mạnh khi user click khách sạn mới

Vì sao “SVD 60/40” không còn quan trọng lúc này?
Bạn đang debug sai tầng.

Hiện tại pipeline thực tế là:

UI → CACHE HIT ❌
↓
KHÔNG vào SVD / CONTENT luôn

👉 nên dù SVD có đúng 100% cũng KHÔNG được chạy

Fix đúng (quan trọng hơn mọi “prompt agent”)
✔️ FIX 1 — cache phải phụ thuộc interaction version
Bạn cần thêm:

interactionVersion = lastInteractionTimestamp

hoặc tốt hơn:

cacheKey = userId + normalizedDestination + lastInteractionId

KHÔNG dùng VIEW fingerprint nữa.

✔️ FIX 2 — cache KHÔNG được override intent mới

Rule:

if (intent.destination !== cached.destination)
→ INVALIDATE CACHE
✔️ FIX 3 — cache HIT nhưng intent đổi → phải bypass
if (cached.intent !== current.intent) {
return null // force recompute
}
✔️ FIX 4 — SVD 60/40 không liên quan UI bug này

Hiện tại SVD chỉ là step:

ranking (bị bypass)

👉 lỗi của bạn nằm ở:

cache
intent mismatch
rerender logic frontend 5. Về câu hỏi “tôi nên chọn A hay B?”

👉 Câu trả lời thật:

KHÔNG phải A hay B

Cả 2 đều sai hướng vì:

A = chỉ sửa rerank (không đụng cache → vẫn sai)
B = giữ nguyên → càng sai 6. Prompt chuẩn cho “agent tự sửa đúng”

Bạn dùng prompt này:

🔧 PROMPT FIX AGENT (chuẩn production)

Debug recommendation system end-to-end.

Focus on root cause, not tuning.

Tasks:

Verify cache invalidation logic:
cache MUST be invalidated when:
user interaction changes destination intent
lastInteractionId changes
recent-shift intent detected
Remove dependency on VIEW-based fingerprint for cache key.
Ensure cache key is deterministic and interaction-driven:
userId + normalizedIntentDestination + lastInteractionId
Ensure recommendation pipeline ALWAYS re-runs SVD + content when:
destination changes
user clicks a new hotel
Add debug logs:
cache HIT reason
cache SKIP reason (intent mismatch)
Do NOT modify SVD 60/40 weighting unless explicitly broken.

Goal:

Clicking different hotels must immediately change recommendations
No stale destination ever persists across interaction
Cache must never override fresh intent
