Hoàn thành toàn bộ cập nhật metrics cho dashboard và AI management. Tổng kết:

## Files đã thay đổi:

### 1. `packages/product-db/prisma/schema.prisma`

- Thêm `mae`, `ndcgAt5`, `baselineRmse`, `baselineMae`, `baselinePrecision`, `baselineRecall`, `baselineNdcg` vào SystemMetric model
- Prisma client regenerated + database synced

### 2. `apps/search-service/generate_mock_interactions.py`

- Cập nhật metric_entry sinh thêm: `mae`, `ndcgAt5`, 5 baseline fields
- Chạy lại thành công: 7875 interactions, 6 signal types

### 3. `apps/admin/src/app/(dashboard)/analytics/page.tsx`

**Trang Analytics (UC-16, UC-21, UC-22)** — Hiển thị tóm tắt:

- 5 KPI cards: RMSE, MAE, Precision@5, Recall@5, NDCG@5
- Mỗi card có công thức ngắn gọn (VD: `√(Σ(ŷ-y)²/N)`)
- Dữ liệu từ `getLatestSystemMetric()` (Prisma DB)

### 4. `apps/admin/src/app/(dashboard)/ai-management/page.tsx`

**Trang AI Management** — Hiển thị chi tiết:

- **SVD Training Metrics**: RMSE, MAE với formula + baseline so sánh
- **System A (Implicit CF)**: Precision@5, Recall@5, NDCG@5 — mỗi card có công thức + baseline
- **System B (Explicit CF)**: RMSE, MAE — formula đầy đủ + baseline
- **Signal Weights Reference**: Hiển thị 6 loại tín hiệu với trọng số

### Phân bổ metrics theo Use Cases:

| Use Case           | Trang hiển thị         | Metrics                       |
| ------------------ | ---------------------- | ----------------------------- |
| UC-06 Xem gợi ý    | Backend (recommend.py) | 6 signal weights → CF ranking |
| UC-16 Xem thống kê | analytics/page.tsx     | RMSE, MAE, P@5, R@5, NDCG@5   |
| UC-21 Giám sát AI  | ai-management/page.tsx | Chi tiết formula + baseline   |
| UC-24 Training     | ai-management/page.tsx | SVD hyperparams, RMSE, MAE    |
