# Hướng dẫn cấu trúc Chapter 3 — Phân biệt 3.1.x vs 3.4.x

## Vấn đề

Bạn có 2 vị trí chứa sơ đồ tương tự nhau:

- **3.1.2** Sơ đồ hoạt động (Activity Diagram)
- **3.1.3** Sơ đồ trình tự (Sequence Diagram)
- **3.4.4** Các luồng hoạt động chính (Main Activity Flows)

## Nguyên tắc phân biệt

### Mức trừu tượng khác nhau:

| Section             | Mức trừu tượng             | Góc nhìn                 | Mục đích                                  | Chi tiết kỹ thuật                        |
| ------------------- | -------------------------- | ------------------------ | ----------------------------------------- | ---------------------------------------- |
| **3.1.x** Phân tích | **CAO** (Business Level)   | Người dùng / nghiệp vụ   | Phân tích "hệ thống phải làm gì"          | KHÔNG có tên service, port, DB           |
| **3.4.x** Thiết kế  | **THẤP** (Technical Level) | Kiến trúc sư / developer | Thiết kế "hệ thống hoạt động như thế nào" | CÓ tên service, port, DB, message format |

---

## 3.1.2. Sơ đồ hoạt động (Activity Diagram) — Business Level

**Nội dung:** Flowchart đơn giản mô tả quy trình nghiệp vụ từ góc nhìn người dùng. Chỉ có Actor và chức năng, KHÔNG có tên service hay database.

**Ví dụ — Luồng đặt phòng (Business View):**

```mermaid
flowchart LR
    A["Khách hàng"] --> B["Tìm kiếm phòng"]
    B --> C["Xem chi tiết"]
    C --> D["Chọn phòng & Nhập thông tin"]
    D --> E{"Phòng trống?"}
    E -->|Có| F["Xác nhận đặt phòng"]
    E -->|Không| B
    F --> G["Thanh toán"]
    G --> H{"Thanh toán thành công?"}
    H -->|Có| I["Nhận xác nhận đặt phòng"]
    H -->|Không| J["Thử lại hoặc hủy"]
    J --> G
```

**Ví dụ — Luồng đăng tin khách sạn (Business View):**

```mermaid
flowchart LR
    A["Chủ khách sạn"] --> B["Tạo thông tin khách sạn"]
    B --> C["Upload hình ảnh"]
    C --> D["Thiết lập giá & tiện nghi"]
    D --> E["Gửi duyệt"]
    E --> F["Admin xem xét"]
    F --> G{"Duyệt?"}
    G -->|Có| H["Khách sạn hiển thị công khai"]
    G -->|Không| I["Trả lại kèm lý do"]
    I --> B
```

**Số lượng:** 4-6 sơ đồ cho các quy trình nghiệp vụ chính (Tìm kiếm, Đặt phòng, Đăng tin, Đánh giá, Chat, Thanh toán).

### Hình 3.3: Luồng xử lý nghiệp vụ đặt phòng thông minh qua AI và thanh toán trực tuyến qua Stripe

```plantuml
@startuml
skinparam DefaultFontName Arial
skinparam DefaultFontSize 13
skinparam ConditionStyle diamond
skinparam shadowing false
skinparam roundcorner 10

start

:Người dùng nhập yêu cầu đặt phòng bằng ngôn ngữ tự nhiên\n(VD: "Tôi muốn đặt villa Đà Lạt từ 20-22/06 cho 2 người");
:AI Agent (FastAPI) tiếp nhận tin nhắn và gửi tới Groq LLM;
:LLM thực hiện Function Calling để trích xuất cấu trúc dữ liệu\n{location, checkIn, checkOut, guests};
:Hệ thống truy vấn Cơ sở dữ liệu để kiểm tra tình trạng phòng trống;

if (Có phòng trống phù hợp?) then (Có)
    :AI trả lời hội thoại kèm theo Card danh sách khách sạn phù hợp;
    :Người dùng chọn phòng và nhấn nút "Xác nhận đặt phòng";
    :Hệ thống kích hoạt Redis Distributed Lock (SETNX)\nđể giữ chỗ phòng trong vòng 15 phút (Chống Overbooking);
    :Khởi tạo đơn đặt phòng với trạng thái PENDING;
    :Tạo phiên thanh toán trực tuyến trên Stripe Checkout;
    :Chuyển hướng người dùng sang trang thanh toán bảo mật của Stripe;
    :Người dùng nhập thông tin thẻ và thực hiện thanh toán;

    if (Stripe xác nhận giao dịch thành công?) then (Thành công)
        :Stripe gửi sự kiện qua Webhook tới Payment Service;
        :Payment Service cập nhật trạng thái giao dịch (SUCCESS)\nvà bắn sự kiện [PAYMENT_SUCCESS] vào Apache Kafka;
        fork
            :Booking Service tiêu thụ (consume) sự kiện,\ncập nhật trạng thái đơn đặt phòng từ PENDING -> CONFIRMED;
            :Giải phóng Redis Lock của phòng;
        fork again
            :Email Worker tiêu thụ sự kiện,\nTự động gửi email xác nhận kèm Vé điện tử (E-ticket);
        end fork
        :Hiển thị màn hình thông báo Đặt phòng thành công;
    else (Thất bại / Quá 15 phút Timeout)
        :Hệ thống tự động hủy đơn đặt phòng (CANCELLED);
        :Xóa Redis Lock để mở lại phòng trống cho người khác;
        :Hiển thị thông báo lỗi thanh toán và hướng dẫn thử lại;
    endif
else (Không)
    :AI Bot thông báo hết phòng theo yêu cầu\nvà đưa ra các lựa chọn gợi ý thay thế;
endif

stop
@enduml
```

### Hình 3.4: Luồng xử lý thu thập tương tác người dùng và tái huấn luyện mô hình gợi ý

```plantuml
@startuml
skinparam DefaultFontName Arial
skinparam DefaultFontSize 13
skinparam ConditionStyle diamond
skinparam shadowing false
skinparam roundcorner 10

start

:Người dùng thực hiện hành động trên ứng dụng\n(Xem phòng, Click, Thêm vào yêu thích, Đặt phòng);
:Hệ thống tự động bắt các tín hiệu tương tác ngầm định và tường minh\n(Implicit Signals & Explicit Signals);
:Lưu trữ dữ liệu tương tác thô vào PostgreSQL;

if (Đến khung giờ 00:00 hàng ngày?) then (Đúng)
    :Kích hoạt Cronjob tiến trình chạy ngầm;
    :Tổng hợp thống kê hiệu suất ngày (daily_stats);
    :Trích xuất toàn bộ dữ liệu tương tác tích lũy của người dùng;
    :Kích hoạt luồng tái huấn luyện mô hình gợi ý SVD (GridSearchCV);
    :Tính toán ma trận tương đồng Item-Based CF mới;
    :Cập nhật và lưu bộ nhớ đệm gợi ý mới vào Redis Cache (TTL = 1 giờ);
else (Chưa đến giờ)
    :Tiếp tục ghi nhận dữ liệu và tích lũy tương tác theo thời gian thực;
endif

stop
@enduml
```

### Hình 3.5: Luồng nghiệp vụ đăng ký đối tác và đăng tin kiểm duyệt khách sạn

```plantuml
@startuml
skinparam DefaultFontName Arial
skinparam DefaultFontSize 13
skinparam ConditionStyle diamond
skinparam shadowing false
skinparam roundcorner 10

start

:Người dùng gửi yêu cầu nâng cấp tài khoản lên Chủ khách sạn (AuthorRequest);
:Quản trị viên (Admin) kiểm tra thông tin pháp lý của đối tác;

if (Phê duyệt yêu cầu nâng cấp?) then (Đạt)
    :Hệ thống cập nhật Role tài khoản từ USER thành AUTHOR;
    :Chủ khách sạn đăng nhập vào Author Portal và tạo tin đăng khách sạn;
    :Nhập thông tin phòng, giá, tiện ích và tải lên hình ảnh;
    :Hệ thống chạy ngầm CLIP Model để encode hình ảnh sang Vector embedding 512d;
    :Lưu thông tin khách sạn ở trạng thái Nháp (DRAFT);
    :Chủ khách sạn kiểm tra lại và nhấn "Gửi duyệt";
    :Trạng thái hệ thống chuyển đổi từ DRAFT -> PENDING;
    :Hệ thống đẩy tin đăng vào danh sách chờ duyệt của Admin;
    :Admin tiến hành kiểm tra thực tế nội dung tin đăng;

    if (Nội dung tin đăng hợp lệ?) then (Hợp lệ)
        :Cập nhật trạng thái khách sạn thành APPROVED;
        :Đồng bộ dữ liệu và hiển thị công khai khách sạn lên sàn Stazy Hotel;
    else (Không đạt yêu cầu)
        :Cập nhật trạng thái khách sạn thành REJECTED;
        :Gửi thông báo phản hồi kèm lý do để Chủ khách sạn tiến hành sửa đổi;
    endif
else (Từ chối)
    :Hệ thống gửi thông báo từ chối nâng cấp tài khoản đối tác;
endif

stop
@enduml
```

---

## 3.1.3. Sơ đồ trình tự (Sequence Diagram) — Business Level

**Nội dung:** Sequence diagram đơn giản chỉ có Actor và "Hệ thống" (1 khối), KHÔNG tách riêng từng service.

**Ví dụ — Luồng đặt phòng (Business View):**

```mermaid
sequenceDiagram
    participant User as 👤 Khách hàng
    participant System as 🏨 Hệ thống Stazy
    participant Payment as 💳 Cổng thanh toán

    User->>System: Tìm kiếm phòng theo địa điểm, ngày
    System-->>User: Danh sách phòng trống

    User->>System: Chọn phòng, nhập thông tin khách
    System->>System: Kiểm tra phòng trống
    System-->>User: Xác nhận thông tin đặt phòng

    User->>System: Xác nhận & Thanh toán
    System->>Payment: Chuyển hướng đến cổng thanh toán
    Payment->>User: Nhập thông tin thẻ
    Payment-->>System: Xác nhận thanh toán thành công

    System->>System: Cập nhật trạng thái đặt phòng
    System-->>User: Gửi xác nhận đặt phòng + Email
```

**Số lượng:** 4-6 sơ đồ cho các quy trình chính.

---

## 3.4.4. Các luồng hoạt động chính — Technical Design Level

**Nội dung:** Sequence diagram CHI TIẾT với tên cụ thể từng service, port, database, message format, middleware. Đây là phần THIẾT KẾ kỹ thuật.

**Ví dụ — Luồng đặt phòng (Technical View):**

```mermaid
sequenceDiagram
    participant User as 👤 Người dùng
    participant Client as 🖥️ Client App<br/>(Next.js :3002)
    participant Booking as 📋 Booking Service<br/>(Fastify :8001)
    participant Redis as 🔒 Redis<br/>(Distributed Lock)
    participant Payment as 💳 Payment Service<br/>(Hono :8002)
    participant Stripe as 🌐 Stripe<br/>(External)
    participant Kafka as 📨 Apache Kafka<br/>(Message Broker)
    participant Email as 📧 Email Service<br/>(Worker :8003)
    participant DB as 🗄️ PostgreSQL<br/>(Prisma)

    Note over User, DB: GIAI ĐOẠN 1: KHỞI TẠO & GIỮ CHỖ
    User->>Client: Chọn phòng, nhập thông tin
    Client->>Booking: POST /bookings
    Booking->>Redis: SETNX booking:{roomId}
    Redis-->>Booking: ✅ Lock acquired
    Booking->>DB: INSERT Booking (status: PENDING)
    Note over Booking: Ghi OutboxMessage
    Booking-->>Client: 200 OK + bookingId

    Note over User, DB: GIAI ĐOẠN 2: XỬ LÝ THANH TOÁN
    Client->>Payment: POST /sessions
    Payment->>Stripe: Tạo Checkout Session
    Stripe-->>Payment: sessionId
    Payment-->>Client: Redirect URL
    Client->>Stripe: Nhập thông tin thẻ

    Note over User, DB: GIAI ĐOẠN 3: ĐỒNG BỘ TRẠNG THÁI
    Stripe->>Payment: POST /webhooks
    Payment->>Kafka: PUBLISH payment-events
    Kafka-->>Booking: CONSUME → UPDATE CONFIRMED
    Booking->>Redis: DEL booking:{roomId}
    Kafka-->>Email: CONSUME → Gửi email xác nhận
```

**Số lượng:** 6-8 sơ đồ cho tất cả luồng kỹ thuật chính.

### 3.4.4.4. Thiết kế luồng gợi ý khách sạn đa chiến lược (Multi-Strategy Recommendation Flow)

Hệ thống Stazy Hotel triển khai hệ thống gợi ý đa chiến lược trong file `src/recommend.py` của Search Service, hỗ trợ 5 chiến lược với cơ chế fallback tự động khi gặp cold-start. Quy trình gợi ý diễn ra realtime khi người dùng truy cập trang "Gợi ý cho bạn".

```mermaid
sequenceDiagram
    participant User as 👤 Người dùng
    participant Client as 🖥️ Client App<br/>(Next.js :3002)
    participant Search as 🔍 Search Service<br/>(FastAPI :8008)
    participant Redis as 🔴 Redis<br/>(Cache Layer)
    participant SVD as 🤖 SVD Model<br/>(Surprise Library)
    participant PG as 🗄️ PostgreSQL<br/>(Interactions + Hotels)

    Note over User, PG: BƯỚC 1 & 2: REQUEST & KIỂM TRA CACHE

    User->>Client: Truy cập trang gợi ý
    Client->>Search: GET /recommend/{userId}<br/>?strategy=svd&top_k=5
    Search->>Redis: GET recommendation:{userId}
    alt Cache Hit
        Redis-->>Search: Cached results
        Search-->>Client: JSON Response
    else Cache Miss
        Redis-->>Search: null
    end

    Note over User, PG: BƯỚC 3: CHỌN CHIẾN LƯỢC & TÍNH ĐIỂM

    alt SVD (Mặc định)
        Search->>SVD: predict(userId, hotelId)
        SVD-->>Search: svd_score (1-5)
        Search->>Search: content_score =<br/>Price(30%) + Location(30%)<br/>+ Amenity(20%) + Category(20%)
        Search->>Search: hybrid = 0.6×SVD + 0.4×Content
    else User-Based CF
        Search->>Search: Cosine Similarity<br/>Top-10 similar users
    else Item-Based CF
        Search->>Search: Item-Item Similarity<br/>từ hotels user đã tương tác
    else Cold-start
        Search->>PG: GET UserPreference
        PG-->>Search: interestedCategories
        Search->>Search: Match category/tags
    end

    Note over User, PG: BƯỚC 4: TRẢ KẾT QUẢ & CACHE

    Search->>Search: Sort by score DESC → Top-K
    Search->>Redis: SET recommendation:{userId}<br/>(TTL = 1 giờ)
    Search-->>Client: JSON {hotels: [...]}
    Client-->>User: 💡 Hiển thị gợi ý cá nhân hóa
```

**Bảng chiến lược gợi ý:**

| Chiến lược     | Thuật toán                     | Trọng số                 | Fallback            |
| -------------- | ------------------------------ | ------------------------ | ------------------- |
| SVD (mặc định) | Matrix Factorization + Content | SVD 60%, Content 40%     | → Content → Popular |
| User-Based CF  | Cosine Similarity (K=10)       | Weighted aggregation     | → Content → Popular |
| Item-Based CF  | Item-Item Similarity           | Σ(sim × rating)          | → Content → Popular |
| Content-Based  | Category/Tag matching          | Score = Σ(match weights) | → Popular           |
| Popular        | reviewStar × reviewCount       | —                        | —                   |

**Phân tích Collaborative Filtering (Đánh giá Offline):**

Hệ thống đánh giá riêng biệt cho 2 loại feedback bằng `evaluate.py`:

- **System A (Implicit CF):** User-Based CF (Cosine Similarity, K=5) trên temporal split (60/20/20). Metrics: Precision@K, Recall@K, NDCG@K. So sánh với Baseline (Top Popular Items).
- **System B (Explicit CF):** User-Based CF (Pearson Correlation, Mean-Centering, K=10). Metrics: RMSE, MAE. So sánh với Baseline (User Mean).

**Huấn luyện SVD (`train_svd.py`):** GridSearchCV trên 24 tổ hợp × 3-fold CV → Train SVD Optimized + Baseline → 5-Fold CV → Save `recsys_model.pkl` + `svd_training_report.json` + `system_metrics` DB.

---

### 3.4.4.5. Thiết kế luồng Content-Based Filtering & Vector Search

Dựa trên sở thích tường minh (Explicit Preferences) và vector embeddings, hệ thống mở rộng khả năng gợi ý bằng Content-Based Filtering kết hợp với Vector Search.

```mermaid
sequenceDiagram
    participant User as 👤 Người dùng
    participant Client as 🖥️ Client App<br/>(Next.js :3002)
    participant Product as ⚙️ Product Service<br/>(Express :8000)
    participant DB as 🗄️ PostgreSQL<br/>(Prisma + pgvector)
    participant Redis as 🔴 Redis<br/>(Cache)
    participant AI as 🔍 Search Service<br/>(FastAPI :8008)
    participant CLIP as 🖼️ CLIP Model<br/>(Sentence Transformers)

    Note over User, CLIP: NGƯỜI DÙNG CẬP NHẬT SỞ THÍCH

    User->>Client: Cập nhật preferences<br/>(categories, amenities, cities)
    Client->>Product: PUT /users/{id}/preferences
    Product->>DB: UPDATE UserPreference
    Product->>Redis: DEL recommendation:{userId}
    Product-->>Client: ✅ Updated

    Note over User, CLIP: GỢI Ý DỰA TRÊN NỘI DUNG (CBF)

    User->>Client: Truy cập trang gợi ý
    Client->>AI: GET /recommend/{userId}?strategy=svd
    AI->>Redis: GET recommendation:{userId}
    Redis-->>AI: Cache miss

    AI->>DB: GET UserPreference<br/>(interestedCategories,<br/>favoriteAmenities, favoriteCities)
    DB-->>AI: User preferences

    AI->>AI: CBF Score =<br/>0.2×Category + 0.2×Amenity<br/>+ 0.3×Location + 0.3×Price

    par Song song
        AI->>AI: CF Score (SVD/User-CF)
    and
        AI->>AI: CBF Score (from preferences)
    end

    AI->>AI: Hybrid = 0.6×CF + 0.4×CBF
    AI->>Redis: SET recommendation:{userId}<br/>(TTL = 1 giờ)
    AI-->>Client: Top-K hotels
    Client-->>User: Gợi ý cá nhân hóa

    Note over User, CLIP: VECTOR SEARCH (Mở rộng)

    User->>Client: Upload ảnh tìm kiếm
    Client->>AI: POST /search/image
    AI->>CLIP: Encode image → vector (512d)
    CLIP-->>AI: Query vector
    AI->>DB: SELECT * FROM hotels<br/>ORDER BY imageVector<br/><=> query_vector LIMIT 10
    DB-->>AI: Similar hotels (pgvector)
    AI-->>Client: Kết quả tìm kiếm
```

**Kiến trúc CBF dựa trên Explicit Preferences:**

| Thành phần      | Trọng số | Nguồn dữ liệu                         | Mô tả                                                |
| --------------- | -------- | ------------------------------------- | ---------------------------------------------------- |
| Category Match  | 20%      | `UserPreference.interestedCategories` | Khách sạn thuộc category user chọn trong onboarding  |
| Amenity Overlap | 20%      | `UserPreference.favoriteAmenities`    | Tỷ lệ tiện nghi trùng khớp giữa user và hotel        |
| Location Match  | 30%      | `UserPreference.favoriteCities`       | Khách sạn ở thành phố user quan tâm                  |
| Price Proximity | 30%      | `UserPreference.avgPriceExpect`       | Khoảng cách giá giữa user expectation và hotel price |

**Vector-Based Content Filtering:**

- **Offline Pipeline:** CLIP model encode hình ảnh → `imageVector` (512d), mô tả → `policiesVector` (512d), lưu vào PostgreSQL với pgvector extension.
- **Online Pipeline:** Cosine Similarity giữa query vector và hotel vectors. Kết hợp với metadata matching.
- **Hybrid Scoring:** `Final = 0.5 × VectorSimilarity + 0.3 × MetadataMatch + 0.2 × Popularity`.

---

### 3.4.4.6. Thiết kế luồng phân tích dữ liệu và báo cáo (Data Analytics & BI Agent)

Hệ thống triển khai BI Agent sử dụng Groq LLM để phân tích dữ liệu kinh doanh tự động, kết hợp với Admin Dashboard để giám sát hiệu suất hệ thống.

```mermaid
sequenceDiagram
    participant Admin as 👮 Admin
    participant Client as 🖥️ Admin Portal<br/>(Next.js :3003)
    participant BI as 🤖 BI Agent<br/>(Python)
    participant LLM as 🧠 Groq LLM<br/>(Llama 3.3)
    participant DB as 🗄️ PostgreSQL<br/>(Prisma)

    Note over Admin, DB: PHÂN TÍCH KINH DOANH BẰNG NGÔN NGỮ TỰ NHIÊN

    Admin->>Client: "Doanh thu tháng này?"
    Client->>BI: POST /bi/chat<br/>{"question": "..."}
    BI->>LLM: Parse intent<br/>(extract: time_range, metric_type)
    LLM-->>BI: Intent: REVENUE_ANALYSIS<br/>range: this_month

    BI->>DB: SELECT date, SUM(revenue),<br/>COUNT(bookings) FROM daily_stats<br/>WHERE date >= '2025-05-01'<br/>GROUP BY date ORDER BY date
    DB-->>BI: Daily metrics data

    BI->>BI: Detect anomalies<br/>(> 2σ from mean)
    BI->>BI: Calculate trends<br/>(week-over-week change)

    BI->>LLM: Generate narrative:<br/>{metrics, anomalies, trends}
    LLM-->>BI: "Doanh thu tháng 5 tăng 15%<br/>so với tháng 4..."

    BI-->>Client: JSON Response<br/>{insights, chart_data,<br/>root_cause,<br/>actionable_suggestion}
    Client-->>Admin: 📊 Báo cáo phân tích
```

**Luồng giám sát hiệu suất hệ thống:**

```mermaid
flowchart LR
    subgraph Monitor["📡 Monitoring Layer"]
        M1["API Response Time<br/>(per endpoint)"]
        M2["Error Rate<br/>(4xx, 5xx)"]
        M3["DB Query Latency<br/>(Prisma metrics)"]
        M4["Cache Hit Rate<br/>(Redis stats)"]
    end

    subgraph Dashboard["📊 Admin Dashboard"]
        CHART1["AppLineChart<br/>(Response time trend)"]
        CHART2["AppBarChart<br/>(Error rate by service)"]
        CHART3["AppPieChart<br/>(Request distribution)"]
        CHART4["SystemMetric<br/>(RMSE, MAE, P@5, NDCG@5)"]
    end

    subgraph Alerts["🔔 Alert Rules"]
        A1["Response time > 2s → Warning"]
        A2["Error rate > 5% → Critical"]
        A3["Cache hit < 70% → Info"]
    end

    Monitor --> Dashboard
    Monitor --> Alerts
```

**BI Agent Architecture:**

- **Intent Parser (Groq LLM):** Phân tích câu hỏi tự nhiên → trích xuất intent (REVENUE_ANALYSIS, HOTEL_PERFORMANCE, USER_BEHAVIOR...).
- **SQL Generator:** Tạo truy vấn PostgreSQL tương ứng với intent.
- **Data Analyzer:** Tổng hợp dữ liệu, phát hiện bất thường (> 2σ), tính xu hướng (week-over-week).
- **Narrative Generator (Groq LLM):** Sinh báo cáo tiếng Việt với `insights`, `root_cause`, `actionable_suggestion`.
- **Dashboard Charts:** AppBarChart, AppLineChart, AppPieChart, AppAreaChart hiển thị dữ liệu realtime từ `daily_stats` table.

---

### 3.4.4.7. Thiết kế luồng đặt phòng và thanh toán bằng Chatbot AI (AI Booking Flow)

Bên cạnh luồng đặt phòng truyền thống (3.4.4.2), hệ thống cho phép người dùng đặt phòng trực tiếp thông qua AI Chatbot. Khi người dùng trò chuyện với AI và表达 ý định đặt phòng (Intent = BOOK), AI Agent sẽ tự động tạo link thanh toán và chuyển hướng người dùng đến cổng thanh toán.

```plantuml
@startuml
' ===== CẤU HÌNH GIAO DIỆN — FONT LỚN & DỄ ĐỌC =====
skinparam defaultFontName Arial
skinparam defaultFontSize 15
skinparam sequenceParticipantFontSize 16
skinparam sequenceActorFontSize 16
skinparam noteFontSize 14
skinparam sequenceMessageAlign center
skinparam maxMessageSize 250
skinparam roundcorner 10
skinparam shadowing false
skinparam linetype ortho
skinparam backgroundColor #FFFFFF

autonumber

' ===== ĐỊNH NGHĨA & MÀU SẮC =====
actor "Khách hàng" as user #FFD54F

box "Client Side" #FFFFFF
    participant "Frontend UI\n(Next.js :3002)" as fe #BBDEFB
end box

box "AI Layer" #FFFFFF
    participant "AI Service\n(FastAPI :8008)" as ai #FFB74D
    participant "Groq LLM\n(Llama 3)" as llm #E1BEE7
end box

box "Business Services" #FFFFFF
    participant "Booking Service\n(Fastify :8001)" as book #FFCC80
    participant "Payment Service\n(Hono :8002)" as pay #FFCC80
end box

box "External" #FFFFFF
    participant "Stripe\n(Payment Gateway)" as stripe #C8E6C9
end box

database "PostgreSQL\n(Prisma)" as db #C8E6C9
database "Redis\n(Lock + Cache)" as redis #FFCDD2

title Sequence Diagram: Đặt phòng & Thanh toán qua AI Chatbot (Stazy)

' ===== BƯỚC 1: USER CHAT VỚI AI =====
== 1. Người dùng trò chuyện với AI Chatbot ==
user -> fe: Chat: "Tôi muốn đặt villa\nĐà Lạt từ 20-22/06\ncho 2 người"
activate fe

fe -> fe: Check Mode\n(AI Agent Booking)

fe -> ai: POST /agent/chat\n{msg, userId, history}
activate ai

ai -> llm: Chat Completion\n(Function Calling)
activate llm
note right of llm
  **Intent Extraction:**
  {
    "intent": "BOOK",
    "location": "Đà Lạt",
    "checkIn": "2025-06-20",
    "checkOut": "2025-06-22",
    "guests": 2
  }
end note
llm --> ai: Return BookingIntent JSON
deactivate llm

' ===== BƯỚC 2: AI TÌM PHÒNG & XÁC NHẬN =====
== 2. AI tìm phòng phù hợp & xác nhận ==
ai -> db: Query available hotels\n(location, dates, guests)
activate db
db --> ai: List[Available Hotels]
deactivate db

ai -> ai: Rank hotels\n(by recommendation score)

ai --> fe: JSON Response\n{reply, hotels: [...],\nui_type: "booking_confirm"}
deactivate ai

fe -> fe: Render UI:\nTin nhắn AI + Card khách sạn\n+ Nút "Xác nhận đặt phòng"
fe -> user: Hiển thị danh sách\nphòng phù hợp

' ===== BƯỚC 3: USER XÁC NHẬN ĐẶT PHÒNG =====
== 3. Người dùng xác nhận & hệ thống giữ chỗ ==
user -> fe: Nhấn "Xác nhận đặt phòng"\n(Chọn khách sạn #3)
fe -> ai: POST /agent/book\n{userId, hotelId,\ncheckIn, checkOut, guests}
activate ai

ai -> book: POST /bookings\n{userId, hotelId,\ncheckIn, checkOut,\nguestName, guestEmail}
activate book

book -> redis: SETNX booking:{roomId}\n(TTL = 15 phút)
activate redis
redis --> book: ✅ Lock acquired
deactivate redis

book -> db: INSERT Booking\n(status: PENDING,\nexpiresAt: +15min)
activate db
db --> book: ✅ bookingId created
deactivate db

note over book
  **Transactional Outbox:**
  Ghi OutboxMessage
  trong cùng transaction
end note

book --> ai: 200 OK + bookingId
deactivate book

ai --> fe: {reply: "Đặt phòng thành công!",\nbookingId, paymentUrl}
deactivate ai

fe -> user: Hiển thị xác nhận\n+ Nút "Thanh toán ngay"

' ===== BƯỚC 4: THANH TOÁN =====
== 4. Thanh toán qua Stripe ==
user -> fe: Nhấn "Thanh toán ngay"
fe -> pay: POST /sessions\n{bookingId, amount}
activate pay

pay -> stripe: Create Checkout Session\n(amount, currency, bookingId)
activate stripe
stripe --> pay: sessionId + redirectUrl
deactivate stripe

pay --> fe: Redirect URL → Stripe
deactivate pay

fe -> user: Chuyển hướng\nđến Stripe Checkout
user -> stripe: Nhập thông tin thẻ\n& Xác nhận thanh toán
stripe -> stripe: Xử lý thanh toán

' ===== BƯỚC 5: ĐỒNG BỘ TRẠNG THÁI =====
== 5. Webhook → Đồng bộ & Hoàn tất ==
stripe -> pay: POST /webhooks\n(Signature verified)\n{PAYMENT_SUCCESS,\nbookingId, amount}
activate pay

pay -> db: UPDATE payment record\n(paymentIntentId, status)
pay -> pay: Publish Kafka event\n{event: PAYMENT_SUCCESS}

pay --> stripe: 200 OK
deactivate pay

note over book, redis
  **Kafka Consumers:**
  Booking: PENDING → CONFIRMED
  Email: Gửi xác nhận
end note

book -> db: UPDATE Booking\n(status: CONFIRMED)
book -> redis: DEL booking:{roomId}\n(Giải phóng lock)

stripe -> user: Chuyển hướng về\ntrang thành công

fe -> fe: Hiển thị:\n"Đặt phòng thành công!\nXác nhận đã gửi email."
fe -> user: ✅ Booking hoàn tất\n+ Email xác nhận
deactivate fe

@enduml
```

**Mô tả luồng đặt phòng qua AI Chatbot:**

| Bước           | Mô tả                                                                                                                                                            | Service xử lý                           |
| -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- |
| 1. Chat với AI | Người dùng nhắn tin cho AI Chatbot với ý định đặt phòng. AI sử dụng Groq LLM Function Calling để trích xuất BookingIntent (location, checkIn, checkOut, guests). | AI Service (FastAPI :8008) + Groq LLM   |
| 2. Tìm phòng   | AI truy vấn PostgreSQL để tìm phòng available theo tiêu chí, sắp xếp theo recommendation score. Trả về danh sách + UI card cho frontend.                         | AI Service + PostgreSQL                 |
| 3. Giữ chỗ     | User xác nhận đặt phòng → AI gọi Booking Service → Redis distributed lock (TTL 15 phút) → INSERT Booking (PENDING) + OutboxMessage.                              | Booking Service (Fastify :8001) + Redis |
| 4. Thanh toán  | Tạo Stripe Checkout Session → Redirect user đến Stripe → Nhập thông tin thẻ → Xác nhận.                                                                          | Payment Service (Hono :8002) + Stripe   |
| 5. Đồng bộ     | Stripe webhook → Payment Service verify signature → Kafka publish → Booking CONFIRMED + Email xác nhận + Giải phóng Redis lock.                                  | Payment + Kafka + Booking + Email       |

**So sánh 2 luồng đặt phòng:**

| Tiêu chí     | Luồng truyền thống (3.4.4.2)               | Luồng AI Chatbot (3.4.4.7)               |
| ------------ | ------------------------------------------ | ---------------------------------------- |
| Entry point  | Trang chi tiết khách sạn → Nút "Đặt phòng" | Chat với AI → AI phát hiện Intent BOOK   |
| Tìm phòng    | User tự browse và chọn                     | AI tự động tìm theo ngôn ngữ tự nhiên    |
| Xác nhận     | Form đặt phòng truyền thống                | AI hỏi lại → User xác nhận qua chat UI   |
| Thanh toán   | Redirect đến Stripe (giống nhau)           | Redirect đến Stripe (giống nhau)         |
| Backend flow | Giống nhau (Redis Lock + Outbox + Kafka)   | Giống nhau (Redis Lock + Outbox + Kafka) |
| UX advantage | Chủ động, kiểm soát chi tiết               | Tiện lợi, tự nhiên, nhanh chóng          |

---

### 3.4.4.8. Thiết kế luồng tìm kiếm bằng hình ảnh tương đồng (Vector Image Search Flow)

Hệ thống Stazy Hotel hỗ trợ người dùng tìm kiếm khách sạn bằng hình ảnh. Người dùng tải lên một ảnh mẫu (VD: phòng ngủ phong cách Scandinavian), hệ thống sử dụng CLIP model (OpenAI) để encode ảnh thành vector 512 chiều, sau đó so sánh cosine similarity với toàn bộ hotel image vectors đã được precompute trong PostgreSQL (pgvector extension) để trả về danh sách khách sạn có phong cách thiết kế tương tự.

```plantuml
@startuml
' ===== CẤU HÌNH GIAO DIỆN — FONT LỚN & DỄ ĐỌC =====
skinparam defaultFontName Arial
skinparam defaultFontSize 15
skinparam sequenceParticipantFontSize 16
skinparam sequenceActorFontSize 16
skinparam noteFontSize 14
skinparam sequenceMessageAlign center
skinparam maxMessageSize 250
skinparam roundcorner 10
skinparam shadowing false
skinparam linetype ortho
skinparam backgroundColor #FFFFFF

autonumber

' ===== ĐỊNH NGHĨA & MÀU SẮC =====
actor "Khách hàng" as user #FFD54F

box "Client Side" #FFFFFF
    participant "Frontend UI\n(Next.js :3002)" as fe #BBDEFB
end box

box "AI Layer" #FFFFFF
    participant "Search Service\n(FastAPI :8008)" as ai #FFB74D
    participant "CLIP Model\n(ViT-B/32)" as clip #E1BEE7
end box

database "PostgreSQL\n(pgvector)" as db #C8E6C9
database "Redis\n(Cache)" as redis #FFCDD2

title Sequence Diagram: Tìm kiếm bằng hình ảnh tương đồng (Vector Image Search)

' ===== BƯỚC 1: USER UPLOAD ẢNH =====
== 1. Người dùng tải lên ảnh tìm kiếm ==
user -> fe: Upload ảnh phòng mẫu\n(VD: phong cách Scandinavian)
activate fe

fe -> fe: Preview ảnh\n(Xác nhận trước khi gửi)

fe -> ai: POST /search/image\nFormData {image: file.jpg}
activate ai

' ===== BƯỚC 2: CLIP ENCODE VECTOR =====
== 2. CLIP encode ảnh thành vector 512 chiều ==
ai -> ai: Preprocess ảnh\n(Resize 224x224, Normalize)

ai -> clip: Encode image\n→ Vector embedding (512d)
activate clip
note right of clip
  **CLIP ViT-B/32:**
  - Input: RGB image 224×224
  - Output: float32[512]
  - Normalize to unit vector
end note
clip --> ai: queryVector[512]
deactivate clip

' ===== BƯỚC 3: VECTOR SIMILARITY SEARCH =====
== 3. Cosine Similarity Search trên PostgreSQL (pgvector) ==

ai -> redis: GET search:image:{hash(image)}
alt Cache Hit (ảnh đã từng tìm kiếm)
    redis --> ai: Cached results
    ai --> fe: JSON Response
else Cache Miss
    redis --> ai: null

    ai -> db: SELECT id, title, address,\nprice, "galleryImgs"[1],\nreviewStar,\n1 - (imageVector <=> $queryVector)\nAS similarity\nFROM hotels\nWHERE status = 'APPROVED'\nAND imageVector IS NOT NULL\nORDER BY imageVector <=> $queryVector\nLIMIT 10
    activate db
    note right of db
      **pgvector Query:**
      <=> operator = Cosine Distance
      similarity = 1 - distance
      Index: ivfflat hoặc hnsw
      trên cột imageVector (vector(512))
    end note
    db --> ai: List[Hotel] with similarity scores
    deactivate db

    ai -> ai: Post-process:\n- Filter similarity > 0.3\n- Merge với metadata\n- Sort by similarity DESC

    ai -> redis: SET search:image:{hash}\n(TTL = 24 giờ)
end

' ===== BƯỚC 4: TRẢ KẾT QUẢ =====
== 4. Hiển thị kết quả tìm kiếm ==
ai --> fe: JSON Response\n{hotels: [{id, title, image,\nprice, similarity, slug...}],\nquery_description: "Scandinavian\nstyle, minimalist, wood tones"}
deactivate ai

fe -> fe: Render UI:\n- Grid ảnh kết quả\n- Hiển thị similarity %\n- Highlight phong cách\n- Nút "Đặt phòng" cho mỗi card

fe -> user: 🖼️ Hiển thị Top-10 khách sạn\ncó phong cách tương tự
deactivate fe

@enduml
```

**Luồng Offline: Precompute Hotel Image Vectors**

Trước khi có thể tìm kiếm, hệ thống cần encode toàn bộ hình ảnh khách sạn thành vectors. Quy trình này chạy offline (batch):

```plantuml
@startuml
skinparam defaultFontName Arial
skinparam defaultFontSize 14
skinparam sequenceParticipantFontSize 15
skinparam roundcorner 10
skinparam shadowing false
skinparam backgroundColor #FFFFFF

autonumber

participant "Admin/Cron" as admin #FFD54F
participant "AI Service\n(FastAPI :8008)" as ai #FFB74D
participant "CLIP Model\n(ViT-B/32)" as clip #E1BEE7
database "PostgreSQL\n(pgvector)" as db #C8E6C9

== 1. Trigger: Khách sạn mới hoặc Batch reindex ==
admin -> ai: POST /admin/reindex-vectors\nhoặc Cron (02:00 hàng ngày)

== 2. Query hotels chưa có vector ==
ai -> db: SELECT id, "galleryImgs"\nFROM hotels\nWHERE status = 'APPROVED'\nAND imageVector IS NULL
db --> ai: List[Hotel images]

== 3. Batch encode ==
loop Mỗi khách sạn
    ai -> ai: Download featuredImage\nvà galleryImgs[0]
    ai -> clip: Encode image\n→ vector (512d)
    clip --> ai: float32[512]
    ai -> db: UPDATE hotels\nSET imageVector = $vector\nWHERE id = $hotelId
end

== 4. Also encode policies text ==
loop Mỗi khách sạn có policies
    ai -> clip: Encode policies text\n→ vector (512d)
    clip --> ai: float32[512]
    ai -> db: UPDATE hotels\nSET policiesVector = $vector\nWHERE id = $hotelId
end

ai --> admin: ✅ Indexed N hotels

@enduml
```

**Mô tả chi tiết:**

| Thành phần                       | Công nghệ                      | Vai trò                                                |
| -------------------------------- | ------------------------------ | ------------------------------------------------------ |
| CLIP Model (ViT-B/32)            | Sentence Transformers (Python) | Encode ảnh/text thành vector 512 chiều                 |
| pgvector Extension               | PostgreSQL Extension           | Lưu vector và thực hiện cosine similarity search       |
| Cosine Distance Operator (`<=>`) | pgvector                       | Tính khoảng cách giữa query vector và hotel vectors    |
| IVFFlat / HNSW Index             | pgvector                       | Index trên vector column để tăng tốc truy vấn O(log n) |
| Redis Cache                      | Redis                          | Cache kết quả tìm kiếm theo hash ảnh (TTL=24h)         |

**Chi phí tính toán:**

| Operation                           | Thời gian            | Tần suất                         |
| ----------------------------------- | -------------------- | -------------------------------- |
| CLIP encode 1 ảnh                   | ~50ms                | Mỗi lần user upload              |
| pgvector cosine search (10K hotels) | ~10-50ms (với index) | Mỗi lần tìm kiếm                 |
| Precompute 1 hotel vector           | ~50ms                | Batch offline (khi có hotel mới) |
| Total latency (user perspective)    | ~100-200ms           | realtime                         |

---

## Bảng so sánh tóm tắt

| Tiêu chí           | 3.1.2/3.1.3 (Phân tích)           | 3.4.4 (Thiết kế)                                    |
| ------------------ | --------------------------------- | --------------------------------------------------- |
| **Mục đích**       | Hiểu nghiệp vụ                    | Triển khai kỹ thuật                                 |
| **Người đọc**      | Giảng viên, người không chuyên    | Developer, kiến trúc sư                             |
| **Tên thành phần** | "Hệ thống", "Khách hàng", "Admin" | "Booking Service (Fastify :8001)", "Redis", "Kafka" |
| **Database**       | Không提到                         | PostgreSQL (Prisma), Redis                          |
| **Message format** | Không提到                         | {"event": "PAYMENT_SUCCESS", "bookingId": "..."}    |
| **Middleware**     | Không提到                         | authMiddleware.ts, shouldBeAdmin                    |
| **Số sơ đồ**       | 4-6 (mỗi loại)                    | 6-8 (tổng)                                          |

---

## Đề xuất cấu trúc Chapter 3

```
3.1. Phân tích thực nghiệm
    3.1.1. Actors & Use Cases          ← update-3-1.md (UC-01 → UC-43)
    3.1.2. Sơ đồ hoạt động            ← 4-6 flowchart đơn giản (Business Level)
    3.1.3. Sơ đồ trình tự             ← 4-6 sequence diagram đơn giản (Business Level)

3.2. Đặc tả yêu cầu hệ thống         ← 3-2.md (FR-01 → FR-28 + NFR)

3.3. Thiết kế cơ sở dữ liệu          ← DBML, ERD

3.4. Thiết kế kiến trúc tổng thể
    3.4.1. Kiến trúc Microservices     ← Tổng quan hệ thống
    3.4.2. Công nghệ sử dụng           ← Tech stack
    3.4.3. Thiết kế Database           ← Schema chi tiết
    3.4.4. Các luồng hoạt động chính   ← 6-8 sequence/flowchart chi tiết (Technical Level)
        3.4.4.1. Xác thực & Phân quyền (Clerk + JWT + RBAC)
        3.4.4.2. Đặt phòng & Thanh toán (Saga + Outbox + Kafka)
        3.4.4.3. AI Chat & Tìm kiếm (RAG + Function Calling)
        3.4.4.4. Gợi ý khách sạn đa chiến lược (Multi-Strategy Recommendation + CF Analysis)
        3.4.4.5. Content-Based Filtering & Vector Search (Explicit Preferences + CLIP)
        3.4.4.6. Phân tích dữ liệu và báo cáo (BI Agent + Dashboard)
        3.4.4.7. Đặt phòng & Thanh toán qua AI Chatbot (AI Booking Flow)
        3.4.4.8. Tìm kiếm bằng hình ảnh tương đồng (Vector Image Search)
    3.4.5. Thiết kế giao diện          ← UI/UX
```
